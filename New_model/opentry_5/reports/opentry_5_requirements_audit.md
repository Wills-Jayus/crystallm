# opentry_5 Requirements Audit

Time: 2026-06-19T14:45:00Z

| requirement | status | evidence |
|---|---|---|
| write only under opentry_5 | pass | New scripts, data, configs, checkpoints, eval, logs, reports, manifests, cache, tmp, and frozen placeholders are under `/data/users/xsw/autodlmini/model/New_model/opentry_5`. |
| use crystallm_env | pass | All Python commands were executed with `conda run -n crystallm_env`. |
| create required directories | pass | `scripts/ configs/ data/ cache/ checkpoints/ logs/ eval/ reports/ manifests/ tmp/ frozen/` exist. |
| set/cache workdir policy | partial | Cache/TMP directories exist under opentry_5. GPU commands must not use shell inline env assignments because they make CUDA unavailable in this environment; scripts set cache variables internally. |
| no ranking / no selector / no energy | pass for current artifacts | No energy checkpoint, scorer, ranker, selector, threshold, or oracle label is used for candidate ordering. E8003 emits no candidate pool. E8009-E8038 use fixed generation_index/seed order and retain invalid candidates in place. |
| no test leakage | pass | No test sample-level CIF/labels/StructureMatcher data were read. Historical E718 aggregate is recorded only as background. |
| canonical evaluation protocol | pass | `configs/canonical_evaluation.yaml`, `reports/canonical_evaluation_protocol.md`, `manifests/canonical_sample_manifest.json`, `manifests/canonical_data_hashes.json`. |
| historical metric scope audit | pass | `reports/historical_metric_scope_audit.md`. |
| no-ranking baseline report | pass | `reports/no_ranking_baseline_report.md`; it explicitly does not claim a new opentry_5 baseline run. |
| grouped split | pass | train_core 41,337; dev_model 6,566; dev_gate 4,724; fold_a 5,388; fold_b 5,902; exact prototype-key leakage count 0. |
| canonical dataset | pass for data foundation | 52,627 canonical records; train CIF copies 41,337; dev CIF copies 11,290; 5 upstream `None` CIFs are marked invalid/excluded from round-trip targets. |
| canonical round-trip | pass on audited subset | `eval/canonical_roundtrip_audit.json`: 2,048 considered, 1 skipped invalid upstream CIF, 2,047/2,047 StructureMatcher successes. |
| OOF W/A condition-gap data | pass for baseline condition dataset | `data/oof_wa_predictions_train.jsonl` 41,337 rows; `data/oof_wa_predictions_dev.jsonl` 11,290 rows; report in `reports/oof_wa_quality_report.md`. |
| corruption/denoising data | pass | train 17,967 examples, dev 3,941 examples; includes synthetic corruption plus train hard negatives only. |
| copy base checkpoint | pass | `checkpoints/base_crystallm_v1_small/ckpt.pt`, sha256 `eeaa5ccb6597da856dfd5007bccea83d40f3bfad10c8837931307fd70a9a0163`. |
| Model A SFT | not complete | Config/report scaffold exists, but no Model A SFT checkpoint has been trained. |
| Model B denoiser | partial | E8003 trained GPU lattice-only smoke model; `checkpoints/model_b_denoiser_smoke/best.pt`; dev MSE ratio 0.040. E8007/E8008 connected it to OrbitEngine for GT-W/A/free-param CIF recovery on fold_a/fold_b with fixed generation_index order. Full predicted-W/A/free-param/site-mapping denoiser not trained yet. |
| Model C multimodal | not complete | Config/report scaffold exists; no CVAE/flow/diffusion/MDN model has been trained. |
| Model D joint generation | partial | MiniCFJoint-v2 grouped fold_a/fold_b gate0 target round-trip smokes passed; fold_a small-overfit wrote a checkpoint but evaluator tail was interrupted. E8009/E8010 completed non-oracle fixed-order full-CIF path-check smokes. E8011/E8012 plain scale-up failed rows>=7. E8013/E8014 rows>=7 curriculum produced one fold_a rows>=7 positive and fold_b rows>=7 W/A/skeleton hits, but fold_b rows>=7 match@20 remained 0. E8015/E8016 added geometry-heavy CE weights; weighted CE alone was not sufficient. E8017/E8018 added heteroscedastic geometry sampling. E8019/E8020 rebalanced symbolic/geometry loss and increased train/model scale; top20 exceeded target on both grouped folds, but rows>=7 match@20 stayed 0 on both folds. E8021-E8026 tested row-pair, expanded, cartesian, stronger cartesian, and previous-coordinate variants without terminal rows>=7 success. E8027 added target-local/nearest-neighbor cartesian loss; fold_b reached 29.69% / 45.31% / 51.56%, exceeding top5/top20 thresholds but still rows>=7 match@20 = 0. E8028 repeated the protocol on fold_a and reached 10.94% / 34.38% / 48.44%, with rows>=7 match@20 = 11.11% and one new rows>=7 positive. E8029 used active-only local separation on fold_b and reached 26.56% / 45.31% / 60.94%, but rows>=7 W/A/skeleton/match@20 all stayed 0. E8030 added complex symbolic loss weighting plus weaker active separation; fold_b reached 21.88% / 31.25% / 54.69%, rows>=7 skeleton@20 = 33.33%, but rows>=7 W/A/match@20 stayed 0. E8031 raised local pair-distance weight and removed active separation; fold_b reached 21.88% / 42.19% / 56.25%, rows>=7 W/A/skeleton@20 = 66.67%, but rows>=7 match@20 remained 0. E8032 loaded the E8031 checkpoint in eval-only mode with lower coordinate/lattice sampling scale; fold_b reached 21.88% / 35.94% / 51.56%, rows>=7 W/A/skeleton@20 = 66.67%, but rows>=7 match@20 remained 0. E8033 added target-relative cartesian separation; fold_b reached 10.94% / 28.12% / 62.50%, rows>=7 W/A/skeleton@20 = 66.67%, but rows>=7 match@20 remained 0. E8034 combined weaker target-relative separation with complex symbolic weighting; fold_b reached 34.38% / 48.44% / 51.56%, exceeding all aggregate thresholds, but rows>=7 match@20 remained 0. E8035 raised target-relative separation to 1.5; fold_b reached 28.12% / 40.62% / 56.25%, so only top20 exceeded the aggregate target and rows>=7 match@20 stayed 0. E8036 loaded the E8034 checkpoint in eval-only mode with coordinate/lattice sampling 0.45/0.25; aggregate thresholds remained passed at 34.38% / 48.44% / 51.56%, but rows>=7 match@20 stayed 0. E8037 added complex-row geometry NLL weighting 1.5; fold_b reached 15.62% / 23.44% / 45.31%, rows>=7 W/A/skeleton@20 = 66.67%, and rows>=7 match@20 remained 0. E8038 reduced the complex geometry weight to 1.2; fold_b reached 14.06% / 28.12% / 57.81%, rows>=7 W/A@20 = 0, and rows>=7 match@20 remained 0. |
| unified K=20 generation protocol | partial | Frozen protocol is written. E8009-E8038 executed K=20 fixed-order smoke subsets, but no full internal gate run has passed. |
| grouped fold/dev_gate/val512 gate | not complete | Target round-trip gate0 smokes passed on fold_a and fold_b. Model B GT-W/A/free-param CIF recovery smoke is directionally positive on fold_a/fold_b, but oracle-conditioned. E8027-E8029 are non-oracle and directionally useful, but grouped-fold behavior is inconsistent: fold_b aggregate can exceed top5/top20 while fold_a aggregate remains below, and rows>=7 success is not replicated. Val512 usage counter remains 0. |
| final frozen best model | not complete | `frozen/best_model` is a placeholder that says no model is frozen. |
| terminal success condition | fail | No paired grouped-fold no-ranking model has exceeded two of match@1/5/20 GT-SG CrystaLLM +5pp targets with rows>=7 consistency, and dev_gate/val512 remain untouched. |

Conclusion: opentry_5 has completed the required data/protocol foundation and multiple fixed-order non-oracle full-CIF smokes, including local-neighbor cartesian variants. E8027/E8028 are directionally useful but inconsistent: fold_b passes aggregate top5/top20 with no rows>=7 match, while fold_a has one rows>=7 positive but does not pass aggregate thresholds. E8029 improves fold_b top20 further with active-only local separation, but collapses rows>=7 symbolic W/A/skeleton hits. E8030 shows that simple complex symbolic weighting does not restore W/A and regresses top1/top5. E8031 restores fold_b rows>=7 W/A/skeleton while keeping top5/top20 above thresholds, but does not create rows>=7 positives. E8032 shows that lowering inference sampling scale from E8031 is insufficient and regresses top5/top20. E8033 shows target-relative separation improves top20 but damages early accuracy and still misses rows>=7. E8034 exceeds all aggregate thresholds but still fails rows>=7. E8035 shows stronger target-relative separation is too strong: top1/top5 regress and rows>=7 still fails. E8036 keeps E8034 aggregate performance under moderate lower sampling but still fails rows>=7; the bottleneck is continuous geometry validity for complex W/A-hit candidates. E8037/E8038 show complex geometry NLL weighting damages early accuracy and does not fix rows>=7. The prompt's stopping condition is not met.
