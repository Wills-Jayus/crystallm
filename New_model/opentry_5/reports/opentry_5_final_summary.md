# opentry_5 Final Summary

Status: not terminal-complete.

Answers required by the final prompt are pending full-scale CIF model training/evaluation. The current artifacts establish protocol, grouped split, canonical targets, OOF W/A condition data, corruption data, no-ranking controls, and a GPU Model B lattice-denoiser smoke checkpoint.

E8009-E8038 added non-oracle fixed-order full-CIF MiniCFJoint smokes on both grouped folds. Candidate order is generation_index plus fixed seed, with invalid candidates retained. E8013 introduced a rows>=7 curriculum and produced the first non-oracle rows>=7 positive on fold_a, while E8014 improved fold_b rows>=7 W/A/skeleton hit but still had rows>=7 match@20 = 0. E8015/E8016 kept that curriculum and increased geometry/lattice CE weights; fold_a improved overall, but fold_b did not replicate. E8017/E8018 changed geometry to heteroscedastic free-param/lattice NLL with fixed-seed sampling. E8019/E8020 then rebalanced symbolic CE and geometry NLL with larger train/model scale. This produced match@20 = 54.69% on fold_b and 51.56% on fold_a, but rows>=7 match@20 stayed 0 on both folds. E8021-E8026 tested row-pair, expanded, cartesian, stronger cartesian, and previous-coordinate variants without terminal rows>=7 success. E8027 added target-local/nearest-neighbor cartesian loss; fold_b reached 29.69% / 45.31% / 51.56%, exceeding top5/top20 thresholds, but rows>=7 match stayed 0. E8028 repeated the same protocol on fold_a; it reached 10.94% / 34.38% / 48.44%, with rows>=7 match@20 = 11.11% and one new rows>=7 positive. E8029 made the fold_b local separation term active-only and reached 26.56% / 45.31% / 60.94%, but rows>=7 W/A/skeleton/match@20 all stayed 0. E8030 added complex symbolic loss weighting plus weaker active separation; it reached 21.88% / 31.25% / 54.69%, with rows>=7 skeleton@20 = 33.33% but W/A/match@20 still 0. E8031 removed active separation and raised local pair-distance weight; it reached 21.88% / 42.19% / 56.25%, with rows>=7 W/A/skeleton@20 = 66.67% but rows>=7 match@20 still 0. E8032 re-evaluated the E8031 checkpoint with lower coordinate/lattice sampling scale; it reached 21.88% / 35.94% / 51.56%, kept rows>=7 W/A/skeleton@20 = 66.67%, and still had rows>=7 match@20 = 0. E8033 added target-relative cartesian separation; it reached 10.94% / 28.12% / 62.50%, kept rows>=7 W/A/skeleton@20 = 66.67%, improved some rows>=7 bond scores, and still had rows>=7 match@20 = 0. E8034 combined weaker target-relative separation with complex symbolic weighting; it reached 34.38% / 48.44% / 51.56%, exceeding all aggregate thresholds, but rows>=7 match@20 remained 0. E8035 increased target-relative separation to 1.5; it reached 28.12% / 40.62% / 56.25%, so top1/top5 regressed and rows>=7 match@20 remained 0. E8036 re-evaluated E8034 with lower coordinate/lattice sampling scale 0.45/0.25; it preserved 34.38% / 48.44% / 51.56%, but rows>=7 match@20 remained 0. E8037/E8038 added complex-row geometry NLL weighting at 1.5 and 1.2; both failed rows>=7, and E8038 lost rows>=7 W/A entirely despite top20 = 57.81%.

The current best non-oracle direction remains the E8034/E8036 balance of local-neighbor geometry, weak target-relative repair, and complex symbolic weighting, but it is not eligible for dev_gate, val512, or freezing because rows>=7 fold_b match is still 0 despite aggregate thresholds being met. E8035 shows that simply strengthening the target-relative separation loss is a regression; E8036 shows that moderate lower inference sampling is insufficient; E8037/E8038 show complex geometry NLL weighting overcorrects or loses rows>=7 W/A.

No candidate reranking or test leakage has been used. `manifests/val512_usage_counter.json` remains at 0.
