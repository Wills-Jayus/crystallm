你接手 SymCIF-v4 / CrystaLLM 实验。

当前不要继续只优化 WA@200。最新 hybrid + orbit-aware rerank 已经完成前端验证：
- WA@20 = 86.60%
- WA@100 = 92.60%
- WA@200 = 94.40%
- Skeleton@200 = 95.60%
- Nonempty = 100%
- Timeout = 0

但现在缺少最关键的 end-to-end reconstruction 结果：match@1 / match@5 / match@20。接下来目标是快速验证当前方法最终效果，并尽快准备一个较大 full-data 模型去和 CrystaLLM-small 对比。

硬性约束：
1. 数据只能使用：
   data/structured_symcif_v4_reextracted
2. 不要使用旧数据：
   data/structured_symcif_v4
3. 不要继续回到 v3 text sampler 做局部修补。
4. 不要继续单独深挖 WA@200 / structured multitask rerank，除非 end-to-end 结果证明 WA ranking 是主瓶颈。
5. 最终必须输出标准 CIF，并使用和 baseline / CrystaLLM-small 一致的 evaluator 计算 match@K 和 RMSE。

请按下面顺序实现和实验。

========================================
任务 A：当前 v4 full pipeline 的 end-to-end eval
========================================

目标：验证当前最新 v4 方法到底能不能超过 baseline。

流程：
formula + fixed SG
→ hybrid WA search
→ orbit-aware rerank fusion
→ topK = 1, 5, 20
→ 使用当前 available geometry retrieval / fallback 生成 CIF
→ 使用统一 evaluator 计算指标

必须输出：
- match@1
- match@5
- match@20
- RMSE
- readable
- valid
- formula_ok
- atom_count_ok
- SG_ok
- bond_length_score / bond_lengths_reasonable
- strict_valid

必须输出 breakdown：
- n_sites >= 6
- num_elements >= 4
- SG=2
- SG=65
- SG=71
- SG=127

输出目录建议：
reports/symcif_v4_full_eval_current/

需要保存：
- full_eval_summary.json
- full_eval_breakdown.csv
- generated_cifs/
- failed_cases.jsonl
- top20_predictions.jsonl

========================================
任务 B：WA upper-bound eval
========================================

目标：判断当前 WA 前端的理论上限。

流程：
predicted WA top20
+ GT geometry / GT free_params / GT lattice
→ render CIF
→ evaluator

必须输出：
- match@1 / match@5 / match@20
- formula_ok / atom_count_ok / SG_ok / readable
- 和任务 A 使用同一批 test samples

目的：
如果任务 B 明显高于任务 A，说明 geometry 是主瓶颈；
如果任务 B 也低，说明 WA search/ranking 仍是主瓶颈。

输出目录：
reports/symcif_v4_wa_upper_bound/

========================================
任务 C：Geometry bottleneck eval
========================================

目标：判断 current geometry retrieval/fallback 到底有多差。

流程：
GT WA
+ current generated/retrieved geometry
→ render CIF
→ evaluator

必须输出：
- match@1 / match@5 / match@20
- readable
- formula_ok
- atom_count_ok
- SG_ok
- collision / bond length 相关指标

目的：
如果 GT WA 下仍然 match@K 很低，说明 geometry 模块是当前最大瓶颈，需要优先做 orbit-level geometry model。

输出目录：
reports/symcif_v4_geometry_bottleneck/

========================================
任务 D：并行启动 full-data 较大模型训练
========================================

目标：快速得到一个可以和 CrystaLLM-small 对比的完整模型。

优先选择已经有 end-to-end 正反馈的路线：
- v2_fixed_cell
或
- v2_constrained

不要优先押还没有 geometry 闭环的 v4 WA-only。

训练要求：
1. 使用 full available train set。
2. 模型规模尽量大于当前小实验模型，但要能在现有 GPU 上稳定训练。
3. 记录完整训练配置：
   - data path
   - model size
   - tokenizer
   - batch size
   - lr
   - epochs / steps
   - seed
   - sampling params
4. 使用相同 evaluator 跑：
   - match@1
   - match@5
   - match@20
   - RMSE
   - valid / readable / formula_ok / SG_ok

对比对象：
- baseline_minprompt
- CrystaLLM-small
- 当前 best v2_fixed_cell / v2_constrained 小实验结果
- 当前 v4 full pipeline 结果

输出目录：
reports/full_model_vs_crystallm_small/

========================================
任务 E：最终 summary
========================================

生成一份总结：
reports/next_step_decision_summary.md

必须包含：
1. 当前 v4 full pipeline 的 match@1 / match@5 / match@20
2. WA upper-bound 和 geometry bottleneck 对比
3. 判断主要瓶颈：
   - WA candidate/search/ranking
   - geometry/free_params/lattice
   - renderer/validator
   - data scale
4. 当前方法是否已经超过 baseline
5. 是否值得继续 v4
6. 是否应优先 full-data 训练 v2_fixed_cell / v2_constrained
7. 下一步最短路径建议

判定规则：
- 如果 current v4 full match@20 已超过 baseline：继续 v4，并补 geometry model。
- 如果 WA upper-bound 高但 full pipeline 低：优先做 orbit-level geometry model。
- 如果 WA upper-bound 也低：继续修 hybrid WA search/rank，但不要只看 WA@200，要看 WA@20 和 match@20。
- 如果 v2 full-data 明显超过 baseline / CrystaLLM-small：优先把 v2 作为论文主结果，v4 作为结构化机制分析或后续增强。
- 如果复杂子集仍差：增加 data scale / oversampling 验证，而不是只做局部规则修补。

请先实现任务 A/B/C，跑出完整结果后再决定是否继续深入 v4 的 geometry model。