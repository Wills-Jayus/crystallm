You are working in Codex Goal Mode.

This is a long-running research-and-engineering campaign. Do not stop after one failed route. Do not treat this as a single script run. Your job is to push SymCIF / Mini-CFJoint from a strong MP-20 result toward a general multi-dataset method that improves match/RMSE across MP-20, MPTS-52, and any other structured crystal-generation datasets already supported in the repo.

Main project:
SymCIF-v4 / Mini-CFJoint-v2 after geometry breakthrough.

Latest confirmed MP-20 result:
The previous campaign successfully integrated row-conditioned e07/e08 geometry into full generation.

Frozen MP-20 test result using `fg03_internal_rowcount_e08`:

* match@1 = 64.30%
* match@5 = 73.52%
* RMSE@1 = 0.0578
* RMSE@5 = 0.0471
* readable@5 = 97.92%
* formula_ok@5 = 97.92%
* atom_count_ok@5 = 98.48%
* SG_ok@5 = 97.92%

This clearly beats the previous split-pipeline reference:

* match@1 = 50.42%
* match@5 = 64.99%
* RMSE@1 = 0.0668
* RMSE@5 = 0.0742

However, the remaining bottleneck is not geometry anymore:

* clean_val WA_hit@5 = 19.43%
* test WA_hit@5 = 15.77%
* rows>=7 clean_val match@5 = 47.20%
* rows>=7 clean_val WA_hit@5 = 4.43%

Interpretation:
The X/L geometry problem has been mostly solved by row-conditioned retrieved geometry modes. The next macro bottleneck is W/A symbolic generation and candidate ranking, especially high row_count / complex structures. The model can often produce matchable structures, but symbolic W/A exactness and ranking are still weak.

Campaign goal:
Build the next-stage macro solution:
A general, multi-dataset, W/A-aware Mini-CFJoint system that improves match/RMSE beyond the current MP-20 result and transfers to other datasets.

You should focus on:

1. W/A symbolic decoder improvement.
2. High row_count / complex subset recovery.
3. Generalization from MP-20 to MPTS-52 and other available datasets.
4. Robust candidate ranking without oracle labels.
5. Unified reproducible evaluation across datasets.
6. Frozen final test only after validation configurations are fixed.

Do not solve this by:

* post-hoc CIF repair
* source_cif_repair
* StructureMatcher label training
* repeated MP-20 test tuning
* merely increasing K without improving candidate quality
* merely adding a ranker patch without improving the generation space
* returning to the old split pipeline as the main method
* optimizing only match@20
* ignoring rows>=7 / atom>=12 complex subsets

Primary output directories:

* reports/symcif_v5_multidataset_wa_decoder/
* runs/symcif_v5_multidataset_wa_decoder/

Existing useful paths:

* model/New_model/symcif_experiment/scripts/run_mp20_fullgen_after_geometry_breakthrough.py
* reports/symcif_v4_mp20_fullgen_after_geometry_breakthrough/
* reports/symcif_v4_mp20_geometry_breakthrough/
* reports/symcif_v4_mp20_minicfjoint_v2/

Use existing code where reasonable, but refactor if needed. The final system should be clean, command-line reproducible, and not a one-off notebook.

PHASE 0 — Repository audit and experiment plan

Inspect the current repo and reports.

Create:

* reports/symcif_v5_multidataset_wa_decoder/00_audit.md
* reports/symcif_v5_multidataset_wa_decoder/01_experiment_plan.md

Audit requirements:

1. Locate current MP-20 full-generation script.
2. Locate e07/e08 geometry implementation and retrieval indexes.
3. Locate WAProposalIndex / internal train-prior exact-cover code.
4. Locate evaluation code for match/RMSE/WA_hit/skeleton_hit/row_count/formula/SG/readable.
5. Discover available datasets in the repo:

   * MP-20
   * MPTS-52
   * any Perov-5 / Carbon-24 / other structured datasets if present
6. For every dataset, identify:

   * train/val/test split path
   * target schema compatibility
   * clean target availability
   * SG/formula fields
   * whether OrbitEngine can render it
   * whether e07/e08 geometry index can be built
7. Write a concrete multi-phase plan.

Important:
If a dataset is not available or not compatible, document it. Do not invent data paths.

PHASE 1 — Build unified multi-dataset runner

Goal:
Generalize the current MP-20 full-generation pipeline into a reusable multi-dataset runner.

Implement or refactor a script such as:

* model/New_model/symcif_experiment/scripts/run_multidataset_wa_decoder_campaign.py

It should support:

* dataset name
* split
* geometry mode: deterministic / e07 / e08 / e07_rank0_e08_diverse
* WA decoder mode
* candidate selector mode
* K values
* max samples
* seed
* output dir
* no-test mode / frozen-test mode

Required:

1. Support MP-20 clean_train/clean_val/test.
2. Support MPTS-52 if data exists.
3. Support any other structured dataset already in the repo if feasible.
4. Build retrieval indexes only from the training split.
5. Never insert val/test samples into retrieval/codebook indexes.
6. Ensure full generation input is only formula + GT_SG unless explicitly diagnostic.
7. Generate standard output json/md reports.

Create smoke tests:

* small MP-20 val subset
* small MPTS-52 val subset if available
* one complex subset smoke test with rows>=7 or atom>=12

Reports:

* reports/symcif_v5_multidataset_wa_decoder/02_runner_smoke_tests.md

PHASE 2 — Reproduce current MP-20 frozen config on val only

Before changing anything, reproduce the current best MP-20 validation behavior.

Run:

* `fg03_internal_rowcount_e08` on MP-20 clean_val
* do not rerun MP-20 test

Expected approximate clean_val:

* match@1 ≈ 72.24%
* match@5 ≈ 82.26%
* RMSE@5 ≈ 0.0436
* valid@5 ≈ 99.45%

If reproduction differs meaningfully:

* debug evaluator path
* debug timeout
* debug direct-CIF evaluator
* debug retrieval index source
* debug random seed/candidate order

Create:

* reports/symcif_v5_multidataset_wa_decoder/03_mp20_reproduction.md

PHASE 3 — W/A symbolic decoder diagnosis

Goal:
Prove exactly where the W/A bottleneck is.

Run diagnostic decomposition on MP-20 clean_val:

D1. current best: predicted W/A + e08
D2. oracle row_count + predicted W/A + e08
D3. predicted W + oracle A + e08, if feasible
D4. oracle W + predicted A + e08, if feasible
D5. oracle skeleton + predicted assignment + e08
D6. generated W/A with larger candidate pool before top5, e.g. pool=20/50/100 but evaluate top1/top5
D7. train-prior W/A retrieval candidates only + e08
D8. neural W/A candidates only + e08
D9. union of neural + train-prior + exact-cover candidates + e08
D10. complex subset only: rows>=7 / atom>=12

For each, report:

* match@1
* match@5
* RMSE@1
* RMSE@5
* WA_hit@1
* WA_hit@5
* skeleton_hit@1
* skeleton_hit@5
* row_count@1
* row_count@5
* formula_closure@1
* formula_closure@5
* readable@5
* formula_ok@5
* atom_count_ok@5
* SG_ok@5
* duplicate WA key rate
* candidate diversity
* rows>=7 metrics
* atom>=12 metrics

Create:

* reports/symcif_v5_multidataset_wa_decoder/04_wa_bottleneck_diagnosis.md
* reports/symcif_v5_multidataset_wa_decoder/04_wa_bottleneck_diagnosis.json

PHASE 4 — Macro W/A decoder improvement campaign

This is the core work. Run at least 12 meaningful W/A experiments across at least 5 strategy families. Do not count tiny LR changes as meaningful experiments unless they test a clear hypothesis.

Strategy Family A — exact-cover symbolic search

Build a stronger formula-constrained W/A generator:

* row_count-aware beam search
* exact-cover feasibility at every step
* legal OrbitToken mask under SG
* multiplicity/formula remaining-state transitions
* canonical row order
* element inventory constraints
* no impossible partial states
* duplicate stable-key deduplication

Goal:
Improve WA_hit@5 and skeleton_hit@5, especially rows>=7.

Experiments:
A1. exact-cover beam with current scoring
A2. exact-cover beam with row_count top-N
A3. exact-cover beam with skeleton-first decoding
A4. exact-cover beam with diversity penalty

Strategy Family B — skeleton-first then assignment

Separate the problem:

1. Generate W/skeleton rows.
2. Assign elements under formula exact-cover.
3. Generate geometry.

Try:

* skeleton decoder conditioned on formula + SG + crystal system
* element assignment as constrained bipartite/exact-cover problem
* top-N skeletons × top-M assignments
* row multiplicity matching
* stable multiset key canonicalization
* assignment diversity under same skeleton

Goal:
Improve skeleton_hit@5 and reduce W/A combinatorics.

Experiments:
B1. skeleton retrieval prior + exact assignment
B2. neural skeleton head + exact assignment
B3. skeleton beam + assignment beam
B4. skeleton candidates from train prior + neural reranking

Strategy Family C — retrieval-augmented W/A generator

Use train-set symbolic memory as a source-level proposal mechanism, not a post-hoc repair.

Build/try:

* formula+SG nearest symbolic keys
* element-set + SG + atom_count matching
* crystal-system-aware WA retrieval
* rows-count-aware retrieval
* skeleton retrieval + assignment adaptation
* train-prior proposals mixed with neural proposals
* deduplicate by stable WA key

Important:
Do not use val/test target WA. Retrieval index must be built from train only.

Goal:
Increase symbolic coverage while preserving generalization.

Experiments:
C1. train symbolic prior only
C2. train prior + exact-cover adaptation
C3. train prior + generated candidate union
C4. train prior + geometry-support ranking

Strategy Family D — complex subset curriculum / expert routing

Rows>=7 and atom>=12 are the current weak points. Treat them as first-class targets.

Try:

* separate W/A decoder or proposal mode for rows>=7
* curriculum fine-tuning on atom>=12 / rows>=7
* complex-weighted loss
* expert routing by row_count bucket
* expert routing by crystal system
* expert routing by formula length / number of elements
* more candidate budget internally for rows>=7, while final metric remains K=1/5

Goal:
Improve rows>=7 match@5 and WA_hit@5 without hurting overall.

Experiments:
D1. complex-weighted training
D2. rows>=7 expert proposal
D3. atom>=12 expert proposal
D4. adaptive internal candidate pool for complex samples

Strategy Family E — geometry-aware symbolic ranking

Use e07/e08 geometry support to rank W/A candidates before final top5.

Try non-oracle ranking features:

* WA model logprob
* exact-cover score
* row_count probability
* skeleton probability
* retrieval frequency
* geometry retrieval distance
* geometry candidate diversity
* lattice plausibility
* volume-per-atom plausibility
* minimum-distance sanity
* SG legality
* formula closure
* duplicate key penalty

Do not train on StructureMatcher match labels. If a learned ranker is used, train it with self-supervised or non-oracle features only.

Goal:
Improve match@1 and match@5 by ranking candidates better.

Experiments:
E1. geometry-distance ranking
E2. hybrid symbolic+geometry ranking
E3. diversity-aware top5 selection
E4. e07 rank0 + e08 diversity + symbolic ranking

Strategy Family F — distillation from successful generated structures

Use generated successful candidates from train split to improve the W/A model without leaking validation/test labels.

Procedure:

1. Run full generation on train or train-subset.
2. Identify internally valid / self-consistent / low-distance / geometry-supported candidates.
3. Distill candidate patterns back into W/A decoder or ranking.
4. Do not use validation/test StructureMatcher labels.
5. Optionally use train labels because train is allowed.

Goal:
Improve W/A generation distribution based on model’s own successful modes.

Experiments:
F1. train successful-candidate replay
F2. self-training W/A decoder on high-confidence train candidates
F3. skeleton distillation from high-confidence train candidates
F4. ranking distillation using non-oracle train features

PHASE 5 — Neural W/A model upgrade

If the current W/A decoder is mostly retrieval/prior-based, implement a stronger neural W/A decoder.

Design:

* formula encoder
* SG embedding
* crystal-system embedding
* row_count head
* skeleton/W decoder
* element assignment decoder
* exact-cover masks
* formula remaining-state embedding
* row-wise transformer or pointer-style decoder
* candidate beam generation
* stable-key deduplication

Requirements:

1. Pass small-overfit on 256 samples.
2. Report row_count/orbit/element/skeleton/WA metrics.
3. Compare against retrieval-prior baseline.
4. Plug into e08 geometry full generation.
5. Evaluate on MP-20 clean_val.

Do not expand to a huge monolithic action softmax. Keep factorized structured heads.

Create:

* reports/symcif_v5_multidataset_wa_decoder/05_neural_wa_model.md

PHASE 6 — Multi-dataset transfer

After MP-20 validation improvements are established, test transfer to other datasets.

Dataset priority:

1. MPTS-52 if available.
2. Other structured datasets already present in repo.

For each dataset:

1. Build clean target/roundtrip audit.
2. Build train-only geometry/WA retrieval indexes.
3. Run baseline current method.
4. Run best MP-20-derived method without tuning.
5. If needed, tune only on that dataset’s train/val.
6. Do not touch test until config is frozen.

Required reports:

* reports/symcif_v5_multidataset_wa_decoder/06_mpts52_val.md
* reports/symcif_v5_multidataset_wa_decoder/07_other_datasets_val.md, if applicable

Metrics:

* match@1
* match@5
* optional match@10/match@20 diagnostic only
* RMSE@1
* RMSE@5
* WA_hit@1
* WA_hit@5
* skeleton_hit@5
* row_count@5
* readable@5
* formula_ok@5
* atom_count_ok@5
* SG_ok@5
* complex subset metrics

Important:
Do not assume MP-20 best hyperparameters transfer. But first run zero-shot transfer, then dataset-specific val tuning.

PHASE 7 — Multi-dataset validation table and method selection

Create:

* reports/symcif_v5_multidataset_wa_decoder/08_multidataset_val_table.md
* reports/symcif_v5_multidataset_wa_decoder/08_multidataset_val_table.json

The table should include:

* dataset
* split
* method
* geometry mode
* W/A decoder mode
* selector mode
* match@1
* match@5
* RMSE@1
* RMSE@5
* valid@5
* WA_hit@5
* skeleton_hit@5
* rows>=7 match@5 if relevant
* atom>=12 match@5 if relevant

Select:

1. Best MP-20 validation config.
2. Best MPTS-52 validation config.
3. Best shared multi-dataset config.
4. Best complex-subset config.
5. Best RMSE config.

PHASE 8 — Frozen test policy

Do not repeatedly test.

MP-20:
The previous frozen test has already been run. Do not rerun MP-20 test unless:

* the new method is clearly different from `fg03_internal_rowcount_e08`
* clean_val improves meaningfully, e.g.

  * match@5 improves by >= 2 pp, or
  * RMSE@5 improves by >= 0.005, or
  * rows>=7 match@5 improves by >= 8 pp without hurting overall
* the config is frozen and documented before test

MPTS-52 or other datasets:
Run frozen test only once per dataset after validation config is frozen.

For every frozen test:

* write config before running
* run once
* do not modify config after seeing result
* save command/config/seed/index/checkpoints/metrics

Create:

* reports/symcif_v5_multidataset_wa_decoder/09_frozen_tests.md
* reports/symcif_v5_multidataset_wa_decoder/09_frozen_tests.json

PHASE 9 — Failure analysis and next direction

Create:

* reports/symcif_v5_multidataset_wa_decoder/10_failure_analysis.md

Analyze:

1. Does W/A_hit improve?
2. Does skeleton_hit improve?
3. Does match improve even if WA_hit does not?
4. Is the WA key metric too strict because equivalent W/A patterns exist?
5. Are failures concentrated in rows>=7?
6. Are failures concentrated in specific SGs?
7. Are failures caused by row_count, skeleton, assignment, ranking, or geometry?
8. Does retrieval generalize across datasets?
9. Does MP-20 tuning transfer to MPTS-52?
10. Does exact-cover improve validity but reduce diversity?
11. Does high internal candidate pool improve top5 or just create duplicates?
12. Does candidate ranking improve match@1 or only preserve match@5?
13. What is the new bottleneck after W/A improvement?

PHASE 10 — Final report

Create:

* reports/symcif_v5_multidataset_wa_decoder/final_report.md

The final report must include:

1. Executive summary.
2. What changed from the previous MP-20 geometry breakthrough.
3. Why geometry is now fixed/frozen.
4. What was done for W/A symbolic generation.
5. Full experiment table.
6. MP-20 validation results.
7. MP-20 frozen test result if rerun under policy.
8. MPTS-52 validation/test results if available.
9. Other dataset results if available.
10. Complex subset results.
11. Match/RMSE comparison against previous references.
12. WA_hit/skeleton_hit analysis.
13. Leakage and test-policy statement.
14. Failure analysis.
15. Recommended next macro direction.

Minimum experiment quota:
Unless there is a hard data/infrastructure blocker, complete at least:

* 1 audit report
* 1 unified runner
* 1 reproduction run
* 10+ MP-20 W/A improvement experiments
* 4+ complex subset experiments
* 4+ candidate ranking experiments
* 1 neural W/A model attempt or clear explanation why infeasible
* 1 MPTS-52 validation campaign if data exists
* 1 multi-dataset validation table
* 1 final report

Important behavior:
Keep going. If exact-cover beam does not improve, try skeleton-first. If skeleton-first fails, try retrieval-augmented W/A. If retrieval helps only match@5, improve ranking. If ranking helps match@1 but hurts match@5, split rank@1 and diversity modes. If rows>=7 stays weak, build a complex expert. If MP-20 improves but MPTS-52 fails, diagnose transfer and dataset-specific constraints. Do not stop after one route.

Success criteria:
A strong result would be:

* MP-20 clean_val match@5 > 84%
* MP-20 clean_val RMSE@5 < 0.043
* rows>=7 match@5 meaningfully above 47.20%
* WA_hit@5 or skeleton_hit@5 meaningfully improved
* MPTS-52 validation improves over previous available baseline
* frozen tests are run only when justified and documented

Even if no further improvement is achieved, the final report must clearly identify whether the bottleneck is:

* W/A symbolic generation
* W/A equivalence metric
* high row_count combinatorics
* candidate ranking
* dataset transfer
* geometry retrieval generalization
* evaluation artifact
