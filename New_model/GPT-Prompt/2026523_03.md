你接手 SymCIF-v4 Table 3 benchmark 修复与重跑任务。

当前背景：
- 旧报告中 MP-20 / MPTS-52 的 WA20 x geom1 full benchmark 结果不能作为最终科学结论，只能作为旧代码 lower-bound。
- 已发现两个关键问题：
  1. run_symcif_v4_geometry_model_eval.py 中 geometry prototype free-param 迁移存在 reference row 复用 bug，会把不同元素放到同一套 Wyckoff free params 上，导致原子重叠、Invalid CIF、readable/strict_valid/match/RMSE 被系统性压低。
  2. full eval 旧代码在 raw candidates rerank/select 前按最终 top_k 提前截断，会破坏 raw top200/top700 → select top20 的策略。
- 当前不要继续训练新大模型，先修复、审计、重跑 benchmark pipeline。
- 数据路径：
  /data/users/xsw/autodlmini/model/scp_task/CrystaLLM/reproduce/benchmarks_gt_from_prepare_csv_benchmark_symprec0p1

核心目标：
修复代码问题后，在 MP-20 和 MPTS-52 上重新评估 SymCIF-v4，并补齐 CrystaLLM+GT-SG same-evaluator baseline，形成可信的 Table 3 对比。

========================================
任务 1：固化代码修复
========================================

必须检查并修复：

1. geometry prototype free-param 迁移 bug
- 禁止不同 WA row / 不同元素复用错误 reference row。
- 每个 element@OrbitToken 必须绑定对应 row 的 free_params。
- 加断言：
  - row element 一致
  - orbit token 一致
  - multiplicity 一致
  - free_param 维度一致
  - expanded atom count 与 formula 一致

2. raw candidate 提前截断问题
- 禁止在 rerank/select 前按最终 top_k 截断。
- 必须支持：
  raw_topK = 200 / 700
  final_selectK = 20
- 每个样本记录：
  raw_candidate_count
  dedup_candidate_count
  final_candidate_count
  truncated_before_rerank
  source_labels

3. 日志和 artifact
- 每次 full eval 必须保存：
  candidates_raw.jsonl
  candidates_reranked.jsonl
  selected_top20.jsonl
  generated_cifs/
  failed_cases.jsonl
  eval_summary.json

输出：
reports/symcif_v4_table3_fix_audit/code_fix_summary.md

========================================
任务 2：first-200 / first-1000 修复复核
========================================

不要直接全量重跑，先做小批量复核。

数据集：
- MP-20 test first-200 / first-1000
- MPTS-52 test first-200 / first-1000

对比配置：
A. old code WA20 x geom1
B. fixed geometry migration WA20 x geom1
C. fixed geometry + fixed raw candidate selection WA20 x geom1

必须输出对比：
- match@1 / match@5 / match@20
- RMSE@1 / RMSE@5 / RMSE@20
- readable
- formula_ok
- atom_count_ok
- SG_ok
- valid / strict_valid
- Invalid CIF count
- missing_candidate count
- render_success
- geometry_source share:
  same_wa
  same_skeleton
  same_sg
  model_fallback
  missing_candidate
- eval_timeout

验收标准：
- Invalid CIF 明显下降
- strict_valid 明显上升
- RMSE 不上升，最好下降
- match@20 至少不低于旧代码

输出：
reports/symcif_v4_table3_fix_audit/first200_first1000/

========================================
任务 3：WA search 审计与恢复强配置
========================================

目标：
确认 benchmark 代码路径没有削弱之前小实验中的 hybrid WA search。

在 MP-20 / MPTS-52 test structured samples 上输出：

- candidate_nonempty
- candidate_count mean / p50 / p90
- timeout
- truncated
- skeleton hit@20 / @100 / @200 / @700
- exact GT-WA hit@20 / @100 / @200 / @700
- structure-equivalent WA hit@20 / @100 / @200 / @700
- source_labels:
  policy
  old_frequency
  fallback
  union
  rerank

必须对比两套 WA search：
A. 当前 formal benchmark WA search
B. 完整 hybrid WA search：
   policy + old frequency + timeout/empty fallback + SG=2 fallback + union dedup + orbit-aware rerank

重点检查：
1. raw candidates 是否真的达到 200/700。
2. 是否仍有 top_k 提前截断。
3. MPTS-52 为什么 truncated 数量很高。
4. GT-WA exact key 是否受 row-order、element-order、origin choice、equivalent Wyckoff 表达影响。
5. 如果 exact GT-WA hit 很低但 structure match 较高，必须报告 structure-equivalent WA hit，不要只用 exact WA key 下结论。

输出：
reports/symcif_v4_table3_fix_audit/wa_search_audit/

========================================
任务 4：修复后 full pipeline 重跑 budget matrix
========================================

修复通过 first-1000 复核后，再跑完整 MP-20 / MPTS-52 test。

必须跑以下配置：

Fair-budget configs:
1. WA20 x geom1
2. WA10 x geom2
3. WA5 x geom4

Best-result configs:
4. WA15 x geom2 → rerank/select top20
5. WA20 x geom2 → rerank/select top20

注意：
- Fair-budget 用于主表公平对比。
- Best-result 必须报告 raw candidate budget，不要伪装成严格 n=20。
- 所有配置最终只用 selected top20 计算 match@20。
- 每个配置都要保存完整 generated CIF 和 eval artifacts。

每个配置必须输出：
- match@1 / match@5 / match@20
- RMSE@1 / RMSE@5 / RMSE@20
- readable
- formula_ok
- atom_count_ok
- SG_ok
- valid / strict_valid
- bond_score / bond_reasonable
- render_success
- eval_timeout
- raw_candidate_budget
- dedup_candidate_count
- selected_candidate_count

复杂子集 breakdown：
- overall
- n_sites >= 6
- n_sites >= 12
- n_sites >= 20
- num_elements >= 4
- rare_sg
- high_multiplicity_orbit
- extraction_hard

输出：
reports/symcif_v4_table3_fixed_full_rerun/{mp20,mpts52}/

========================================
任务 5：补 CrystaLLM + GT-SG same-evaluator baseline
========================================

目标：
补齐公平对比。published Table 3 的 CrystaLLM a 是 composition-only，而 SymCIF-v4 使用 composition + oracle GT SG，因此必须补一个 CrystaLLM+GT-SG baseline。

要求：
- 找到 MP-20 / MPTS-52 对应 CrystaLLM benchmark/base checkpoint。
- 对 test set 使用 cell composition + GT SG prompt。
- 每个样本生成 n=20。
- 使用和 SymCIF-v4 完全相同 evaluator。
- 记录 sampling params：
  temperature
  top_k
  max_new_tokens
  seed
  n
- 如果 checkpoint 不可用，必须记录搜索路径和失败原因，不要假装完成。

输出指标：
- match@1 / match@5 / match@20
- RMSE@1 / RMSE@5 / RMSE@20
- readable
- formula_ok
- SG_ok
- valid / strict_valid
- eval_timeout

输出：
eval_runs/crystallm_gt_sg_same_evaluator/{mp20,mpts52}/

========================================
任务 6：denominator 与泄露控制
========================================

正式报告必须同时给两套分母：

1. structured-only score
- 只在成功结构化的 test samples 上计算。
- 用于内部诊断。

2. full-test score
- 原始 test split 全部分母。
- 结构化失败样本按 failure 计入。
- 用于和 published Table 3 对比。

必须报告：
- original test count
- structured test count
- structured success rate
- failed structured sample ids
- failure reasons
- train/val/test 是否严格隔离
- 是否使用 train+val 训练；如果使用，必须标注 non-Table3-comparable

输出：
reports/symcif_v4_table3_fixed_full_rerun/denominator_and_leakage_audit.md

========================================
任务 7：最终 summary
========================================

生成：
reports/symcif_v4_table3_fixed_full_rerun/final_summary.md

必须包含：

1. 代码修复摘要
- geometry reference row bug 是否修复
- raw top_k 提前截断是否修复
- first-200 / first-1000 修复前后对比

2. WA search 审计
- exact GT-WA hit@20/100/200/700
- structure-equivalent WA hit@20/100/200/700
- skeleton hit@20/100/200/700
- candidate_nonempty / timeout / truncated

3. 修复后 full pipeline 结果
- MP-20：WA20x1 / WA10x2 / WA5x4 / WA15x2 / WA20x2
- MPTS-52：同上
- 每个配置给 match@1/5/20 和 RMSE@1/5/20

4. Published Table 3 comparison
- CrystaLLM a composition-only published
- SymCIF-v4 + oracle GT SG
- 明确标注：不是严格同输入公平对比

5. SG-conditioned fair comparison
- CrystaLLM + GT SG
- SymCIF-v4 + GT SG
- same split
- same n=20
- same evaluator
- same timeout policy

6. denominator
- structured-only score
- full-test score

7. 复杂子集
- n_sites>=6
- n_sites>=12
- n_sites>=20
- num_elements>=4
- rare_sg
- high_multiplicity_orbit
- extraction_hard

8. 最终判断
- MP-20 是否超过 CrystaLLM a published
- MPTS-52 是否超过 CrystaLLM a published
- SG-conditioned fair comparison 下是否仍有优势
- 如果未超过，主要瓶颈是 WA search、geometry、renderer，还是复杂子集
- 下一步是否需要模型级改造或只需要扩大数据/预算

执行顺序：
1. 任务 1
2. 任务 2
3. 任务 3
4. 任务 4
5. 任务 5
6. 任务 6
7. 任务 7

注意：
在任务 1-3 通过前，不要继续训练新模型，也不要基于旧 WA20xgeom1 结果判断 SymCIF-v4 失败。