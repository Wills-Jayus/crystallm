你接手 SymCIF-v4 MP-20 top1/top5 rerank 任务。

当前硬性限制：
1. 只做 MP-20。
2. 不要跑 MPTS-52。
3. 不要跑 match@20。
4. 不要生成新的 top20 evaluation。
5. 不要重新 render。
6. 不要训练新大模型。
7. 只输出 match@1 / match@5 / RMSE@1 / RMSE@5 和相关质量指标。
8. 已有 top20 artifacts 可以作为候选池读取，但不能新增 match@20 实验或报告 match@20 主表。

现有目录：
reports/symcif_v4_table3_fixed_full_rerun_stablekey_hybrid/mp20/wa5_geom4_stablekey_hybrid/

已有文件：
- top20_predictions.jsonl
- metrics.jsonl
- metrics/baseline_per_generation_metrics.jsonl
- failed_eval_cases.jsonl
- full_eval_summary.json
- artifact_audit.json

背景结果：
- current full-test match@1 = 50.42%
- current full-test match@5 = 64.99%
- current structured-only match@1 = 51.29%
- current structured-only match@5 = 66.11%
- published CrystaLLM a MP-20 match@1 = 55.85%
- CrystaLLM Table 3 没有 match@5，因此 match@5 作为内部 gate。

任务 1：读取现有 artifacts，重建 candidate-level table
每一行是一个 generated candidate，字段至少包括：
- sample_id
- candidate_rank
- wa_rank
- geom_variant_id
- geometry_source
- render_success
- readable
- formula_ok
- atom_count_ok
- SG_ok
- valid
- strict_valid
- match
- rms
- WA_hit / skeleton_hit if available
- failure reason if available

不要重新跑 StructureMatcher。

任务 2：只计算 top1/top5 replay 指标
输出 current ordering 的：
- structured-only match@1 / match@5
- structured-only RMSE@1 / RMSE@5
- full-test match@1 / match@5
- full-test RMSE@1 / RMSE@5
- readable@1 / @5
- formula_ok@1 / @5
- atom_count_ok@1 / @5
- SG_ok@1 / @5
- strict_valid@1 / @5
- eval_timeout@1 / @5
- WA_hit@1 / @5
- skeleton_hit@1 / @5

禁止输出 match@20 主表。

任务 3：做 top5 budget subset replay
从已有 wa5_geom4 的 20 个候选中模拟以下 final K=5 策略：
- WA5 x geom1
- WA3 x geom2 select top5
- WA2 x geom3 select top5
- WA1 x geom4
- current WA5 x geom4 current top5

只输出 match@1 / match@5 / RMSE@1 / RMSE@5 / strict_valid@1/@5。
不要重新 render。

任务 4：做 lightweight rerank replay
实现 2-3 个不使用 test match label 训练的简单 scorer，例如：
A. current order baseline
B. validity-first scorer:
   strict_valid/readable/formula_ok/SG_ok 优先，然后 WA score / candidate rank
C. geometry-quality scorer:
   bond/min-distance/lattice/free-param proxy 优先，然后 WA score
D. hybrid scorer:
   WA rank + geometry quality + strict_valid proxy

注意：
- 不允许用 test 的 match label 调参。
- 如果没有 train/val candidate-level labels，就只做 rule-based scorer。
- 每个 scorer 只评估 top1/top5。
- 记录 scorer 是否使用了 evaluator-derived fields；如果使用，只标记为 oracle/diagnostic，不作为真实 inference 结果。

任务 5：错误分解只看 top1/top5
输出：
- top1 failed reason breakdown
- top5 failed reason breakdown
- failed_with_WA_hit@5
- failed_without_WA_hit@5
- failed_with_skeleton_hit@5
- invalid_cif@5
- matcher_no_match@5
- geometry_source breakdown@5
- complex subset breakdown@5:
  - n_sites>=6
  - n_sites>=12
  - num_elements>=4
  - rare_sg
  - high_multiplicity_orbit
  - extraction_hard

任务 6：生成报告
输出：
reports/symcif_v4_table3_fixed_full_rerun_stablekey_hybrid/mp20/wa5_geom4_stablekey_hybrid/top1_top5_rerank_replay_summary.md
reports/symcif_v4_table3_fixed_full_rerun_stablekey_hybrid/mp20/wa5_geom4_stablekey_hybrid/top1_top5_rerank_replay_summary.json

报告必须回答：
1. current top1/top5 是否比旧 WA20xgeom1 明显更好？
2. top5 预算应该更多给 WA diversity 还是 geometry diversity？
3. 是否存在简单 rerank 能提升 match@1 / match@5？
4. 当前 match@1 距 CrystaLLM a MP-20 55.85% 还差多少？
5. 当前 RMSE@1 / RMSE@5 是否仍明显偏高？
6. 下一步应该跑哪个 K<=5 的小实验？
7. 是否已经达到扩大到 MPTS-52 或重新考虑 match@20 的条件？

停止规则：
- 不要启动任何新 render。
- 不要启动 MPTS-52。
- 不要启动 CrystaLLM baseline。
- 不要跑 match@20。
- 完成 top1/top5 replay summary 后停止。