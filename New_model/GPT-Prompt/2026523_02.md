你接手 SymCIF-v4 正式 benchmark 实验。当前目标不是继续小规模修补，而是在 CrystaLLM Table 3 的 MP-20 和 MPTS-52 benchmark 上训练并评测 SymCIF-v4，分别和 CrystaLLM a 做指标对比。

背景：
- 当前 v4 小实验 best full pipeline 已达到 match@20=62.8%，说明 v4 主线有效。
- 下一步要迁移到正式 benchmark：
  - MP-20
  - MPTS-52
- 数据路径：
  /data/users/xsw/autodlmini/model/scp_task/CrystaLLM/reproduce/benchmarks_gt_from_prepare_csv_benchmark_symprec0p1
- 该路径下是完整 CIF 文件，需要做后处理，生成 SymCIF-v4 结构化数据。
- 注意：CrystaLLM Table 3 的 CSP benchmark 默认输入是 cell composition；SymCIF-v4 需要 cell composition + GT SG。所有报告必须明确标注是否使用 GT SG。

Table 3 published CrystaLLM a 目标数值：
MP-20:
- match@1 = 55.85%
- match@20 = 75.14%
- RMSE@1 = 0.0437
- RMSE@20 = 0.0395

MPTS-52:
- match@1 = 17.47%
- match@20 = 32.98%
- RMSE@1 = 0.1113
- RMSE@20 = 0.1197

硬性要求：
1. 不要训练或评测时泄露 test split。
2. “全量训练”指使用 benchmark 官方 train split 的全部样本；不要把 test 加进训练。
3. 如果使用 train+val 训练 final model，必须单独标记为 non-Table3-comparable。
4. 所有评测使用同一套 StructureMatcher / RMSE 逻辑：
   - match@1
   - match@5
   - match@20
   - RMSE@1
   - RMSE@5
   - RMSE@20
   - readable
   - valid / strict_valid
   - formula_ok
   - atom_count_ok
   - SG_ok
   - eval_timeout
5. 每个数据集都要保存完整 artifacts 和 summary。

========================================
任务 1：冻结 v4 generation budget 配置
========================================

在 MP-20 和 MPTS-52 上都固定以下 full pipeline 配置：

Fair-budget configs:
- WA20 x geom1
- WA10 x geom2
- WA5 x geom4

Best-result configs:
- WA15 x geom2 → rerank/select top20
- WA20 x geom2 → rerank/select top20

要求：
- Fair-budget configs 用于主表公平对比。
- Best-result configs 用于工程最佳结果，但必须报告 raw candidate budget。
- 所有配置最终只用 top20 计算 match@20。
- 输出不同 budget 配置的 match@1/5/20、RMSE、valid、timeout。

输出：
reports/symcif_v4_benchmark_budget_ablation/{mp20,mpts52}/

========================================
任务 2：优化 renderer/evaluator throughput
========================================

目标：
正式 benchmark 评测不能被 timeout 干扰。

实现：
- 对 CIF parse、SG detection、StructureMatcher 分别记录耗时。
- 输出 timeout sample ids 和 timeout reason。
- 缓存 GT Structure、composition、SG、normalized structure。
- 对重复 generated CIF 做 hash 去重。
- 对明显 formula/atom_count 不匹配的样本提前跳过 StructureMatcher。
- 保持 evaluator 逻辑和 Table 3 对齐，不要为了提速改变 match 判定。

必须输出：
- eval_timeout@1/5/20
- parse_timeout
- sg_timeout
- matcher_timeout
- timeout breakdown by n_sites / num_elements / SG

输出：
reports/symcif_v4_benchmark_evaluator_throughput/

========================================
任务 3：MP-20 / MPTS-52 数据后处理与结构化数据构建
========================================

从以下路径读取完整 CIF：
/data/users/xsw/autodlmini/model/scp_task/CrystaLLM/reproduce/benchmarks_gt_from_prepare_csv_benchmark_symprec0p1

需要自动识别：
- dataset = MP-20 / MPTS-52
- split = train / val / test
- CIF path / material id / formula if available

对每个 CIF 做后处理：
1. 用 pymatgen 读取结构。
2. 提取 conventional cell composition，禁止使用 reduced formula 作为 exact-cover target。
3. 用 symprec=0.1 提取 SG number / H-M symbol。
4. 用 OrbitEngine 提取 WA-table：
   element @ OrbitToken
5. 提取 row-level free_params 和 lattice。
6. 生成 SymCIF-v4 structured records。
7. 记录失败原因：
   - parse fail
   - SG detect fail
   - OrbitEngine fail
   - WA extraction fail
   - free_params extraction fail
   - formula mismatch
   - occupancy/disorder issue

必须输出每个数据集的数据审计：
- train/val/test 样本数
- structured success rate
- SG distribution
- n_sites distribution
- num_elements distribution
- n_sites>=6 / num_elements>=4 占比
- failed cases jsonl

输出：
data/structured_symcif_v4_mp20/
data/structured_symcif_v4_mpts52/
reports/symcif_v4_benchmark_data_audit/{mp20,mpts52}/

========================================
任务 4：复杂子集 tracking 与加权策略
========================================

在 MP-20 和 MPTS-52 上都必须跟踪复杂子集：

Subsets:
- n_sites >= 6
- n_sites >= 12
- n_sites >= 20
- num_elements >= 4
- rare SG
- high multiplicity orbit samples
- extraction hard cases

训练时至少跑两组：
1. no weighting
2. complex weighting

complex weighting 至少覆盖：
- n_sites>=6
- num_elements>=4
- rare SG
- high orbit-count samples

每组都要输出三段损失：
- WA hit@20
- GT-WA + geometry model match@20
- predicted-WA + geometry model full pipeline match@20

目的：
判断复杂结构到底卡在 WA search、geometry model，还是 predicted-WA 和 geometry 的耦合。

输出：
reports/symcif_v4_benchmark_complex_subsets/{mp20,mpts52}/

========================================
任务 5：MP-20 / MPTS-52 全量训练与 CrystaLLM a 对比
========================================

目标：
分别训练两个 dataset-specific SymCIF-v4 模型：
- SymCIF-v4-MP20
- SymCIF-v4-MPTS52

训练数据：
- 只使用对应 benchmark 的 train split。
- val split 用于 early stopping / model selection。
- test split 只用于最终评测。

模型：
- WA module: hybrid WA search + orbit-aware rerank
- geometry module: orbit-level geometry model
- 使用任务 1 中冻结的 budget configs。
- 优先保留 WA15 x geom2 best config，同时必须保留 fair-budget config。

同时调用 CrystaLLM base model 做 SG-conditioned baseline：
- 对 MP-20 test set 调用对应 CrystaLLM benchmark/base model。
- 对 MPTS-52 test set 调用对应 CrystaLLM benchmark/base model。
- Prompt 使用 cell composition + GT SG。
- 每个 test sample 生成 n=20。
- 使用同一 evaluator 计算 match@1/5/20 和 RMSE。
- 如果找不到 checkpoint，记录路径搜索结果，并只保留 published Table 3 对比。

报告中必须有三类对比：

A. Published Table 3 comparison:
- CrystaLLM a composition-only published result
- SymCIF-v4 + GT SG result
- 明确标注：SymCIF uses oracle GT SG, not strictly same input.

B. SG-conditioned fair comparison:
- CrystaLLM a/base + GT SG
- SymCIF-v4 + GT SG
- same split
- same n=20
- same evaluator
- same timeout policy

C. Internal ablation:
- WA20 x geom1
- WA10 x geom2
- WA5 x geom4
- WA15 x geom2
- no weighting vs complex weighting

最终输出表必须包含：
For MP-20:
- Table3 CrystaLLM a match@1/match@20/RMSE
- CrystaLLM+SG reeval match@1/5/20/RMSE
- SymCIF-v4 fair match@1/5/20/RMSE
- SymCIF-v4 best match@1/5/20/RMSE
- delta vs CrystaLLM a published
- delta vs CrystaLLM+SG

For MPTS-52:
同上。

输出目录：
runs/symcif_v4_mp20_full/
runs/symcif_v4_mpts52_full/
eval_runs/symcif_v4_mp20_table3_compare/
eval_runs/symcif_v4_mpts52_table3_compare/
reports/symcif_v4_table3_mp20_mpts52_comparison/

最终报告：
reports/symcif_v4_table3_mp20_mpts52_comparison/summary.md

summary.md 必须回答：
1. MP-20 上是否超过 CrystaLLM a published。
2. MPTS-52 上是否超过 CrystaLLM a published。
3. 加入 GT SG 后 CrystaLLM baseline 是否变强。
4. SymCIF-v4 和 CrystaLLM+SG 是否公平对比下仍有优势。
5. MP-20 / MPTS-52 的主要瓶颈分别是什么。
6. 复杂子集是否仍是 MPTS-52 的主要风险。
7. 是否可以进入 final full-scale experiment。
8. 下一步最短路径。