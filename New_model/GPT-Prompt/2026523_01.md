你接手 SymCIF-v4 下一阶段实验。

当前结论：
- v4 current full pipeline 已经超过 baseline：
  - v4 current: match@1=34.8%, match@5=52.0%, match@20=56.6%
  - baseline same-as-v4: match@20=44.6%
  - baseline_minprompt same-as-v4: match@20=47.8%
- WA upper-bound match@20=82.4%，说明 WA 前端上限较高。
- GT-WA + current geometry match@20=56.4%，几乎等于 v4 current 56.6%，说明当前主瓶颈是 geometry/free_params/lattice。
- v2_full_large match@20=47.2%，低于 v4 current，且明显过拟合；v2 暂时只保留为 control，不作为主线。

硬性要求：
1. 主线继续 v4，不要继续深挖 WA@200 / standalone ranker / structured multitask rerank。
2. 不要回到 v2/v3 文本 sampler 做局部修补。
3. 数据优先使用：
   data/structured_symcif_v4_reextracted
4. 所有结果必须使用 same evaluator，输出 match@1 / match@5 / match@20 / RMSE / readable / formula_ok / atom_count_ok / SG_ok / valid / strict_valid。
5. 正式结果必须能输出标准 CIF。

========================================
任务 1：修 renderer / evaluator 兼容问题
========================================

问题：
当前 v4 expanded CIF 的 valid/strict_valid=0，主要是 legacy evaluator 对 OrbitEngine expanded CIF 的 multiplicity loop 检查不适配。

目标：
让 v4 生成 CIF 在 evaluator 中不再因为格式/loop 不兼容被全部打成 valid=0。

要求：
- 检查 generated CIF 与 legacy evaluator 的字段要求。
- 适配或补齐 multiplicity / atom_site / symmetry 相关 loop。
- 保持 StructureMatcher、formula、SG 检查逻辑不变。
- 输出修复前后对比：
  - readable
  - formula_ok
  - atom_count_ok
  - SG_ok
  - valid
  - strict_valid
  - match@1/5/20

输出目录：
reports/symcif_v4_renderer_evaluator_fix/

========================================
任务 2：训练 orbit-level geometry model
========================================

目标：
替代 current geometry retrieval/fallback，预测 lattice + row-level free_params。

输入：
- formula
- SG
- WA-table row token / element@OrbitToken
- orbit metadata, including multiplicity/site_symmetry/enumeration if available

输出：
- lattice parameters
- row-level canonical free_params

建议 loss：
- lattice loss：按 crystal system 做合理归一化
- free_param loss：periodic/wrapped coordinate loss
- optional collision/bond penalty proxy
- optional SG/render closure validation loss or post-check

第一阶段只做 controlled eval：
GT WA + generated geometry → render CIF → evaluator

核心目标：
把 GT-WA + current geometry 的 match@20 从 56.4% 提升到 70%-75%+。

必须输出：
- match@1 / match@5 / match@20
- RMSE
- readable / formula_ok / atom_count_ok / SG_ok
- valid / strict_valid
- bond_score / bond_reasonable
- breakdown:
  - overall
  - n_sites>=6
  - num_elements>=4
  - SG=2
  - SG=65
  - SG=71
  - SG=127

输出目录：
reports/symcif_v4_geometry_model_gtwa/

========================================
任务 3：接入 predicted WA top20，跑 full pipeline
========================================

当任务 2 的 GT-WA geometry model 有提升后，接入完整 v4 pipeline：

formula + fixed SG
→ hybrid WA search
→ orbit-aware rerank fusion top20
→ geometry model
→ render CIF
→ evaluator

必须和当前 v4 current 对比：
- current v4 full pipeline: match@20=56.6%
- WA upper-bound: match@20=82.4%
- new v4 + geometry model: match@1/5/20

目标：
优先冲过 match@20=60%-65%，如果能接近 70% 更好。

输出目录：
reports/symcif_v4_full_pipeline_geometry_model/

========================================
任务 4：复杂子集 data scale / oversampling 验证
========================================

当前复杂子集很弱：
- n_sites>=6 current match@20=13.1%
- n_sites>=6 WA upper-bound=61.1%
- n_sites>=6 GT-WA/current-geometry=17.7%

请做针对性实验：
1. 训练时提高复杂样本采样权重：
   - n_sites>=6
   - num_elements>=4
   - rare SG: 65 / 71 / 127
2. 对比 no-oversampling vs oversampling。
3. 检查提升来自：
   - WA hit 提升
   - geometry 提升
   - renderer/valid 提升

输出目录：
reports/symcif_v4_complex_subset_oversampling/

========================================
任务 5：最终决策报告
========================================

生成：
reports/symcif_v4_geometry_next_step_summary.md

报告必须包含：
1. renderer/evaluator 修复是否成功。
2. GT-WA + generated geometry 是否超过 current geometry。
3. full pipeline + geometry model 是否超过当前 v4 current 56.6%。
4. 复杂子集是否改善。
5. 是否已经具备正式大规模实验条件。
6. 是否可以开始和 CrystaLLM-small 做正式同条件对比。
7. 下一步最短路径建议。

判定规则：
- 如果 renderer/evaluator 修复后 valid/strict_valid 正常，且 full pipeline match@20 > 60%，继续扩大 v4 geometry model。
- 如果 GT-WA geometry 明显提升但 full pipeline 提升有限，回头检查 WA top20 与 geometry 输入不匹配问题。
- 如果 GT-WA geometry 也不提升，说明 geometry model 设计或数据不足，优先修 geometry 表达和 loss。
- 如果复杂子集仍差，继续做 data scale / oversampling，而不是手写局部规则。
- v2 路线暂时只作为 baseline/control，不作为主线继续投入大规模训练。