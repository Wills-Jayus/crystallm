You are working in Goal Mode.

Project context:
We are continuing SymCIF-v4 / Mini-CFJoint work. The previous split pipeline has reached a useful but insufficient ceiling. The current MP-20 test result for wa5_geom4_stablekey_hybrid is approximately:
- full-test match@1 = 50.42%
- full-test match@5 = 64.99%
- full-test match@20 = 69.08%
- RMSE@1 = 0.0668
- RMSE@5 = 0.0742

This does NOT beat CrystaLLM-a published MP-20 match@1 = 55.85%, and RMSE is clearly worse. The current method also uses composition + oracle GT_SG, so it already has a more favorable input condition than composition-only baselines. Do not optimize for a cosmetic comparison.

Important strategic decision:
Do NOT continue the main line by patching rankers, rerank replay, oracle diagnostic scorers, one-fix candidate selection, CIF repair, or match@20 chasing. Those are split-pipeline fixes. The goal is to build Mini-CFJoint-v2 as a source-level structured joint model that predicts/generates the coupled structure variables directly.

High-level goal:
Build and validate Mini-CFJoint-v2:
P(W, A, X, L | formula, GT_SG)

where:
- W = Wyckoff/orbit/skeleton decision
- A = atom/element assignment
- X = free fractional coordinates / internal positional parameters
- L = lattice/cell parameters

The model should replace the fragile pipeline:
WA search -> candidate selection -> geometry prototype/fallback -> CIF render

with a structured multi-head / hierarchical generation model.

Scope restrictions:
- Use MP-20 train/val only.
- Do NOT use MP-20 test for development or tuning.
- Do NOT run MPTS-52.
- Do NOT run CrystaLLM baseline.
- Do NOT optimize match@20.
- Only evaluate K = 1 and K = 5 during this stage.
- Do NOT use source_cif_repair or post-hoc CIF repair to hide target/model failures.
- Do NOT keep or recreate the old 58,880-class element@OrbitToken monolithic softmax.
- Do NOT proceed to full generation if small-overfit fails.
- Do NOT proceed to full generation if GT-WA geometry fails.
- Do NOT expand model size before proving the target format and small-overfit gate.

Output directories:
- reports/symcif_v4_mp20_minicfjoint_v2/
- runs/symcif_v4_mp20_minicfjoint_v2/

The latest uploaded experiment log may be corrupted or non-Markdown. If present in the repo, verify whether it is readable before using it. Do not infer experimental conclusions from unreadable binary/log artifacts.

Main task:
Design, implement, and validate Mini-CFJoint-v2 as a macro-level replacement for the split pipeline. You should inspect the existing repo, reuse robust components where appropriate, but you are allowed to refactor interfaces, target construction, data modules, model heads, decoding, and evaluation scripts if that is required for a clean architecture.

Do not solve this by adding narrow patches around the current pipeline. Solve it by making the representation, supervision targets, heads, constraints, and decoding coherent.

Required phases and gates:

Phase 0 — Repository audit and plan
1. Inspect the existing codebase for:
   - SymCIF-v4 renderer / OrbitEngine / Wyckoff utilities
   - Mini-CFJoint-v1 code
   - MP-20 train/val loading code
   - current evaluation scripts for match/RMSE/WA_hit/readable/formula_ok/atom_count_ok/SG_ok
2. Write a short implementation plan to:
   - reports/symcif_v4_mp20_minicfjoint_v2/plan.md
3. The plan must explicitly identify:
   - what v1 components are safe to reuse
   - what v1 components must be replaced
   - where the 58,880-class softmax or equivalent bad action design exists
   - how v2 will factor the target space
   - how each gate will be run and reported

Phase 1 — Clean target and roundtrip gate
Goal:
Create a clean, canonical Mini-CFJoint-v2 target representation from MP-20 train/val.

The target must be structured, not a flat next-token CIF string. It should separate at least:
- row_count / number of orbit rows
- orbit or Wyckoff row identity under GT_SG
- element assignment per row
- multiplicity / formula accounting
- free coordinate parameters per row
- lattice parameters
- masks / legal choices / constraints needed for decoding

Requirements:
1. Implement target builder and serializer.
2. Implement target -> CIF/render roundtrip using the existing reliable renderer where possible.
3. Implement validation metrics:
   - readable
   - formula_ok
   - atom_count_ok
   - row_expansion_ok if available
   - SG_ok
   - formula_closure
   - invalid/missing target reasons
4. Run on MP-20 train/val.
5. Save:
   - target schema docs
   - aggregate metrics
   - failure examples
   - top recurring failure categories

Gate 0 pass criteria:
- GT roundtrip readable/formula_ok/atom_count_ok/SG_ok >= 99.5%
- formula_closure >= 99%
- no reliance on source_cif_repair

If Gate 0 fails:
- Do NOT train the model.
- Try at least two macro-level target/schema fixes, not ad-hoc post-hoc CIF repair.
- Examples of acceptable macro fixes:
  - canonicalizing equivalent Wyckoff row ordering
  - fixing multiplicity/formula accounting at target construction
  - changing the target decomposition
  - adding legal masks from OrbitEngine
- Re-run Gate 0.
- If still failing, stop and write a failure report explaining the target defects.

Phase 2 — Hierarchical action space / model design
Goal:
Replace Mini-CFJoint-v1’s bad monolithic action space with a structured multi-head model.

Required design:
1. Do NOT use a single huge action softmax over element@OrbitToken.
2. Factor decisions into smaller heads, for example:
   - row_count head
   - orbit/Wyckoff head with GT_SG-conditioned legal mask
   - element head conditioned on formula remainder and row/orbit context
   - coordinate/free-parameter regression or distribution head
   - lattice/cell regression or distribution head
   - optional validity/stop/mask heads if useful
3. Ensure all heads can be trained and evaluated separately.
4. Ensure decoding can enforce hard constraints:
   - legal Wyckoff choices for the space group
   - formula closure or near-closure
   - atom count consistency
   - K <= 5 candidate generation
5. Prefer a clean architecture over minimal changes to v1.

Deliverables:
- model code
- config
- training script
- eval script
- README or report explaining the factorization
- unit tests or smoke tests for target/model batch construction

Phase 3 — 256-sample small-overfit gate
Goal:
Prove the target/model/training loop is sane before scaling.

Procedure:
1. Select a fixed 256-sample subset from MP-20 train.
2. Train Mini-CFJoint-v2 until overfit or until a clear failure is diagnosed.
3. Track per-head train metrics, not just total loss:
   - row_count accuracy
   - orbit top1/top5
   - element top1/top5
   - formula_closure
   - coordinate loss
   - lattice loss
   - render validity
4. Save learning curves, configs, checkpoint, and decoded examples.

Gate 1 pass criteria:
- orbit top1 >= 95%
- element top1 >= 95%
- row_count_accuracy >= 95%
- formula_closure >= 99%
- rendered GT-like decoded samples are readable and formula/atom-count valid

If Gate 1 fails:
- Do not scale up.
- Try at least three meaningful macro-level attempts before stopping. Do not only tune learning rate.
- Acceptable attempts include:
  - revising target factorization
  - adding legal masks
  - changing row ordering/canonicalization
  - changing conditioning between heads
  - separating row_count/orbit/element supervision more cleanly
  - fixing loss weighting if one head dominates
  - simplifying the model to prove memorization
- After each attempt, write what changed and why to:
  reports/symcif_v4_mp20_minicfjoint_v2/small_overfit_attempts.md
- If still failing, stop and produce a failure diagnosis. Do not proceed.

Phase 4 — GT-WA geometry gate
Goal:
Evaluate whether the geometry heads can generate good X/L when W/A is given or teacher-forced. This isolates geometry from WA generation.

Procedure:
1. Use GT-WA / teacher-forced W/A from the clean target.
2. Predict/generate X and L.
3. Render CIFs and evaluate K=1 and K=5 on MP-20 val.
4. Report:
   - match@1
   - match@5
   - RMSE@1
   - RMSE@5
   - readable
   - formula_ok
   - atom_count_ok
   - SG_ok
   - failure breakdown by n_sites, n_sites>=6, n_sites>=12, and extraction_hard if labels exist

Gate 2 pass criteria:
- match@1 >= 60%
- match@5 >= 70%
- RMSE@1 <= 0.09
- RMSE@5 <= 0.08

If Gate 2 fails:
- Do not proceed to full generation.
- Try at least two macro-level geometry design attempts before stopping.
- Acceptable attempts include:
  - changing X/L parameterization
  - predicting normalized lattice parameters
  - separate lattice and free-coordinate heads
  - mixture/distributional outputs for geometry diversity
  - conditioning geometry on orbit/element/SG embeddings more explicitly
  - improving loss scaling between coordinate and lattice terms
- Do not use post-hoc geometry repair or prototype fallback to fake success.
- Record all attempts and metrics.

Phase 5 — Constrained full generation K<=5
Only enter this phase if Gate 0, Gate 1, and Gate 2 pass.

Goal:
Run full Mini-CFJoint-v2 generation from formula + GT_SG on MP-20 val.

Procedure:
1. Generate K=1 and K=5 candidates.
2. Use constrained decoding with legal masks and formula closure.
3. Do not use split-pipeline WA search as the generator.
4. Do not use old candidate selection/ranker as the main solution.
5. Render CIFs and evaluate:
   - match@1
   - match@5
   - RMSE@1
   - RMSE@5
   - WA_hit@1
   - WA_hit@5
   - readable
   - formula_ok
   - atom_count_ok
   - SG_ok
   - formula_closure
   - invalid reason categories
   - breakdown by n_sites, n_sites>=6, n_sites>=12, extraction_hard if available

Gate 3 target:
- match@1 should approach or exceed the current split-pipeline baseline of 50.42%
- match@5 should be at least 55% as an initial usable result
- WA_hit@5 and formula_closure should be reported explicitly

If Gate 3 fails:
- Diagnose whether the bottleneck is:
  - row_count
  - orbit/Wyckoff selection
  - element assignment
  - formula closure
  - geometry X
  - lattice L
  - renderer/validity
  - complex subset collapse
- Try at least two macro-level decoding/model attempts, not ranker patches.
- Acceptable attempts include:
  - better constrained decoding
  - factorized beam search over row_count/orbit/element
  - formula-remainder-aware element decoding
  - separate geometry diversity sampling
  - stronger conditioning between W/A and X/L
- Do not use MP-20 test.

Phase 6 — Final report
Write a final report to:
reports/symcif_v4_mp20_minicfjoint_v2/final_report.md

The report must include:
1. What was implemented.
2. What was deliberately not done.
3. Gate 0/1/2/3 pass/fail status.
4. Metrics table for every gate.
5. All attempted macro-level fixes and their outcomes.
6. Clear comparison against current split-pipeline baseline:
   - match@1 = 50.42%
   - match@5 = 64.99%
   - RMSE@1 = 0.0668
   - RMSE@5 = 0.0742
7. Whether Mini-CFJoint-v2 is promising enough to justify a frozen MP-20 test run later.
8. Failure examples and root-cause analysis.
9. Next recommended action.

Working style:
- Be autonomous.
- Prefer coherent architecture over local patches.
- Try multiple macro-level approaches when a gate fails.
- Do not stop after one failed run unless the repo/data is fundamentally missing.
- Keep all runs reproducible with configs, seeds, commands, and output paths.
- Every script should be callable from the command line.
- Every generated report should include the exact command used.
- Keep test set untouched.

Success definition:
The best outcome is not merely a small metric bump. The best outcome is a clean Mini-CFJoint-v2 implementation with reliable targets, passing small-overfit, passing GT-WA geometry, and a first constrained full-generation result on MP-20 val. If it fails, the failure report must identify the true architectural bottleneck rather than hiding it with downstream repair.