你现在执行 SymCIF-v4 下一阶段。不要跑 full match@5，不要回到 v3 文本 sampler。当前目标是提升 Gate 2 WA search coverage，并启动结构化多任务 WA 模型。

背景：
- 使用 `data/structured_symcif_v4_reextracted`，不要使用旧 structured_symcif_v4。
- Gate 1 已通过：readable/formula_ok/atom_count_ok/row_expansion_ok=100%，SG_ok=99.65%。
- step-level policy search 已把 WA top200 从 85.8% 提到 92.6%，WA top100=90.8%，但 skeleton top200=94.2%，timeout=9，SG=2/复杂结构仍弱。
- retrieved_geometry 未过线，暂时不要跑 full match@5。

任务 1：实现 hybrid WA search
- 合并 policy search 和 old frequency search。
- 对 timeout、candidate_nonempty=false、SG=2、n_sites>=6、num_elements>=4 样本自动启用 fallback。
- 输出 policy-only、old-only、fallback、union 四套 topK 指标。
- 对 union candidates 做 dedup，并保留 source 标签。
- 评估 top20/top100/top200：
  - skeleton recall
  - WA recall
  - timeout
  - candidate_nonempty
  - n_sites>=6
  - num_elements>=4
  - SG=2/65/71/127

任务 2：实现 orbit-aware listwise reranker
- 输入 hybrid union candidates。
- positive = GT WA。
- negatives = same formula+SG 的 hard negatives。
- hard negatives 包括：
  1. same skeleton wrong assignment
  2. same multiplicity pattern wrong site_symmetry
  3. same SG same nsites wrong orbit token
  4. policy high-score wrong candidate
  5. old-search high-score wrong candidate
- 模型可以先用 Set Transformer / no-positional Transformer，不要只用 MLP frequency features。
- 输出 WA top1/top5/top20/top100/top200。

任务 3：加入结构化多任务 loss 的 WA-only 版本
Loss:
  L_total =
    1.0 * L_step_policy
  + 0.5 * L_WA_listwise_rank
  + 0.2 * L_skeleton_set
  + 0.1 * L_value_reachable

说明：
- `L_step_policy`: masked legal-action CE，预测下一步 element@OrbitToken。
- `L_WA_listwise_rank`: candidate set CE，GT WA vs hard negatives。
- `L_skeleton_set`: 预测 skeleton orbit multiset，不依赖 train template id。
- `L_value_reachable`: 判断 partial state 是否能通向 GT / valid high-quality WA。
- 暂时不要加入 coords/lattice loss。
- next-token CE 可选，只作为小权重辅助，不作为主目标。

任务 4：优化 search runtime
- 加 batch scoring / logits cache。
- cache key 包括 SG、remaining_counts、used_fixed_orbits、candidate action set。
- 对 SG=2 低对称样本尝试降低 branching_factor 或使用 old DP fallback。
- 报告 runtime p50/p90/max 和 timeout 样本。

验收目标：
- hybrid/rerank WA top200 >= 95%
- WA top100 >= 92%
- WA top20 > 83%
- skeleton top200 >= 97% 作为短期目标
- n_sites>=6 WA top200 >= 88%
- timeout <= 1
- 输出完整报告，不跑 full match@5。

输出：
- reports/symcif_v4_hybrid_search/hybrid_search_summary.json
- reports/symcif_v4_hybrid_search/hybrid_breakdown_per_sg.csv
- reports/symcif_v4_hybrid_search/hybrid_breakdown_per_nsites.csv
- reports/symcif_v4_hybrid_search/test_hybrid_candidates.jsonl
- reports/symcif_v4_hybrid_search/test_reranked_predictions.jsonl
- reports/symcif_v4_hybrid_search/summary.md

禁止：
- 不要训练 coords/lattice。
- 不要跑 full match@5。
- 不要使用旧 free_params 数据。
- 不要用 post-hoc repair 伪装提升。