你接手 SymCIF-v4 Table 3 修复后最小闭环任务。

当前不要展开新实验，不要同时跑多个 benchmark，不要跑 full budget matrix，不要继续 debug CrystaLLM+GT-SG baseline。只做一个目标：完成 MP-20 `wa5_geom4_stablekey_hybrid` 的 full eval，并生成可信 summary。

背景：
- 旧 Table 3 结果是 provisional lower-bound，不能作为最终结论。
- 已修复：
  1. geometry prototype free-param reference row 复用 bug；
  2. raw candidates 在 rerank/select 前被 top_k=20 提前截断的问题；
  3. WA/skeleton key order-sensitive 问题，已改为 stable / multiset-equivalent key。
- full-test WA audit 显示 MP-20 multiset-equivalent WA@20=82.60%，WA@700=96.40%，所以当前先不要继续怀疑 WA search。
- 现在最缺的是：stablekey 修复后的 MP-20 full pipeline 全量 match/RMSE。

硬性限制：
1. 只做 MP-20。
2. 只做配置：
   `wa5_geom4_stablekey_hybrid`
3. 不要跑 MPTS-52。
4. 不要跑 CrystaLLM+GT-SG baseline。
5. 不要训练新模型。
6. 不要重新 render，除非发现现有 artifact 缺失或损坏。
7. 如果 artifact 缺失，只补缺失 shard，不要全量重跑。

现有 artifact 目录：
`/data/users/xsw/autodlmini/model/New_model/symcif_experiment/reports/symcif_v4_table3_fixed_full_rerun_stablekey_hybrid/mp20/wa5_geom4_stablekey_hybrid/`

预期已有：
- generated_cifs dirs: 8893
- baseline.jsonl lines: 177860
- top20_predictions lines: 8893
- metric lines: 0
- eval_summary: missing

========================================
任务 1：artifact 审计
========================================

检查并输出：
- generated_cifs 样本目录数
- baseline.jsonl 行数
- top20_predictions.jsonl 行数
- 每个 sample 是否有 20 个 selected candidates
- 是否存在空 CIF / 缺失 CIF / 重复 CIF
- 是否存在已经完成的 metric 文件
- 是否可以直接 resume eval

输出：
`artifact_audit.md`
`artifact_audit.json`

如果发现少量缺失：
- 只补缺失 sample / shard。
- 不要重跑全部 render。

========================================
任务 2：resume evaluator
========================================

基于现有 top20_predictions / generated_cifs 直接跑 evaluator。

要求：
- resume-safe
- per-sample sharding
- 支持中断后继续
- 不重复评测已完成 metrics
- 记录 parse / SG / matcher / sample timeout
- 对明显 formula/atom_count 不匹配的候选可提前 skip StructureMatcher，但不要改变最终 match 判定逻辑
- worker 数量先用安全值，例如 32 或 48；如果稳定再提高，不要盲目 96 workers

必须输出：
- metrics.jsonl
- failed_eval_cases.jsonl
- timeout_breakdown.csv
- full_eval_summary.json

========================================
任务 3：计算 structured-only 和 full-test 两套分数
========================================

MP-20 test denominator：
- original test count = 9046
- structured test count = 8893
- structured success rate = 98.31%

必须输出两套结果：

A. structured-only：
- denominator = 8893
- 用于内部诊断

B. full-test：
- denominator = 9046
- 结构化失败样本按 failure 计入
- 用于和 published Table 3 更接近地对比

每套都输出：
- match@1
- match@5
- match@20
- RMSE@1
- RMSE@5
- RMSE@20
- readable@1/5/20
- formula_ok@1/5/20
- atom_count_ok@1/5/20
- SG_ok@1/5/20
- valid@1/5/20
- strict_valid@1/5/20
- eval_timeout@1/5/20
- render_success@1/5/20

========================================
任务 4：错误分解
========================================

输出 breakdown：

Subsets:
- overall
- n_sites >= 6
- n_sites >= 12
- n_sites >= 20
- num_elements >= 4
- rare_sg
- high_multiplicity_orbit
- extraction_hard

Failure categories:
- missing_candidate
- invalid_cif
- parse_fail
- formula_mismatch
- atom_count_mismatch
- SG_mismatch
- matcher_no_match
- timeout
- duplicate_candidate
- geometry_source category:
  same_wa
  same_skeleton
  same_sg
  model_fallback
  missing_candidate

重点回答：
- full match@20 低在哪里？
- 是 geometry 质量问题，还是 candidate/rerank 问题？
- topK 增大后 RMSE 是否变差？
- readable / strict_valid 是否比旧 WA20xgeom1 明显改善？

========================================
任务 5：最终小报告
========================================

生成：
`reports/symcif_v4_table3_fixed_full_rerun_stablekey_hybrid/mp20/wa5_geom4_stablekey_hybrid/mp20_wa5_geom4_final_summary.md`

报告必须包含：

1. 本次只跑 MP-20 / wa5_geom4 的原因。
2. artifact 是否完整，是否复用了已有 render。
3. structured-only 结果表。
4. full-test 结果表。
5. 和旧 WA20xgeom1 lower-bound 对比：
   - old MP-20 WA20xgeom1 match@20=68.92%
   - old RMSE@20=0.0848
6. 和 published CrystaLLM a MP-20 对比：
   - CrystaLLM a MP-20 match@1=55.85%
   - CrystaLLM a MP-20 match@20=75.14%
   - CrystaLLM a MP-20 RMSE@1=0.0437
   - CrystaLLM a MP-20 RMSE@20=0.0395
   - 明确标注：SymCIF 使用 composition + oracle GT SG，published CrystaLLM a 是 composition-only，不是严格同输入公平对比。
7. 错误分解。
8. 下一步判断。

判定规则：
- 如果 structured-only match@20 >= 75.14%：
  说明 MP-20 structured subset 上已经达到/超过 published CrystaLLM a match@20；下一步再看 full-test score 和 RMSE。
- 如果 full-test match@20 >= 75.14%：
  这是强 positive signal，下一步可以跑 MP-20 另一个配置 WA20x1 或 WA10x2 做确认。
- 如果 match@20 在 72%-75%：
  路线接近成功，下一步只跑 MP-20 WA10x2 或 WA15x2，不要碰 MPTS-52。
- 如果 match@20 < 70%：
  不要跑新 config，先做 failure audit，重点检查 geometry_source、strict_valid、formula_ok、SG_ok 和 complex subset。
- 如果 RMSE@20 明显高于 CrystaLLM a：
  即使 match@20 接近，也必须报告结构质量差距，下一步优先做 geometry scoring/rerank，而不是继续增加 WA candidates。

完成后停止，不要自动启动其他 benchmark 或 config。