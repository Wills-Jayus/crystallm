你接手 SymCIF-v4 MP-20 K<=5 root-cause audit 与小规模模型/数据修正任务。

当前硬性限制：
1. 只做 MP-20。
2. 不要跑 MPTS-52。
3. 不要跑 match@20。
4. 不要继续做 oracle/diagnostic rerank 作为主线。
5. 不要训练大模型。
6. 不要基于 MP-20 test 反复调参；后续开发使用 MP-20 val，test 只做最终确认。
7. 所有新实验只报告：
   - match@1
   - match@5
   - RMSE@1
   - RMSE@5
   - WA_hit@1
   - WA_hit@5
   - skeleton_hit@1
   - skeleton_hit@5
   - readable@1/@5
   - formula_ok@1/@5
   - atom_count_ok@1/@5
   - SG_ok@1/@5
   - strict_valid@1/@5

背景：
当前 MP-20 test wa5_geom4_stablekey_hybrid 结果：
- structured match@1 = 51.29%
- structured match@5 = 66.11%
- WA_hit@1 = 49.80%
- WA_hit@5 = 71.27%
- n_sites>=6 match@5 = 26.73%
- extraction_hard match@5 = 38.69%

判断：
当前不是单纯排序问题。top5 失败中 failed_without_WA_hit 明显多于 failed_with_WA_hit，说明 final top5 WA 覆盖不足；复杂子集和 geometry source 也有明显数据/模型问题。

========================================
任务 1：切换到 MP-20 val dev set
========================================

建立 MP-20 val 的 K<=5 dev evaluation pipeline。

要求：
- 不要跑 match@20。
- 不要调 test。
- 复用现有模型/checkpoint/artifacts，必要时只补 val 的 K<=5 render/eval。
- 输出 current baseline on val：
  - match@1 / match@5
  - RMSE@1 / RMSE@5
  - WA_hit@1 / WA_hit@5
  - skeleton_hit@1 / skeleton_hit@5
  - readable / formula_ok / atom_count_ok / SG_ok / strict_valid
  - breakdown:
    - n_sites>=6
    - n_sites>=12
    - num_elements>=4
    - rare_sg
    - high_multiplicity_orbit
    - extraction_hard

输出：
reports/symcif_v4_mp20_k5_dev/val_baseline_summary.md
reports/symcif_v4_mp20_k5_dev/val_baseline_summary.json

========================================
任务 2：final top5 WA coverage audit
========================================

目标：
确认 final top5 WA 覆盖不足的具体原因。

在 MP-20 val 上输出：
- selected WA_hit@1 / @5
- selected skeleton_hit@1 / @5
- raw candidate WA_hit@5 / @10 / @50 / @100，只做 WA key audit，不跑 StructureMatcher
- raw candidate skeleton_hit@5 / @10 / @50 / @100
- GT WA 在 raw candidates 中但没进 selected top5 的样本数
- GT WA 不在 raw candidates 中的样本数
- complex subset 上同样统计

重点回答：
1. 是 raw search 没找到，还是 selected top5 没选进去？
2. n_sites>=6 / extraction_hard 的 WA miss 是哪一种？
3. final top5 的 WA diversity 是否不足？
4. 是否存在 same-skeleton wrong-assignment 的系统错误？

输出：
reports/symcif_v4_mp20_k5_dev/wa_top5_coverage_audit.md
reports/symcif_v4_mp20_k5_dev/wa_top5_coverage_audit.json

========================================
任务 3：same_wa / geometry source audit
========================================

当前 test 结果里 same_wa attempts 极少，same_sg attempts 很多且命中率低。请在 MP-20 val 上审计原因。

检查：
1. train prototype index 中 same_wa key 覆盖率。
2. candidate WA key 和 train prototype WA key 是否 stablekey 一致。
3. same_wa miss 是否来自：
   - element order
   - row order
   - orbit token setting
   - multiplicity mismatch
   - site symmetry mismatch
   - train 中真的不存在该 WA
4. same_sg fallback 占比和 match 率。
5. same_skeleton source 占比和 match 率。
6. geometry source 对 match@1/@5 和 RMSE@1/@5 的影响。

输出：
reports/symcif_v4_mp20_k5_dev/geometry_source_audit.md
reports/symcif_v4_mp20_k5_dev/geometry_source_audit.json

========================================
任务 4：GT-WA geometry K<=5 controlled eval
========================================

目标：
把 WA 问题和 geometry 问题分开。

只跑 MP-20 val：
- GT WA + current geometry model
- K=1 / K=5 only
- 不跑 match@20

输出：
- match@1 / match@5
- RMSE@1 / RMSE@5
- readable / formula_ok / atom_count_ok / SG_ok / strict_valid
- n_sites>=6 / n_sites>=12 / extraction_hard breakdown
- geometry source breakdown

判定：
- 如果 GT-WA match@5 很高，但 predicted-WA full match@5 低，主问题是 WA top5 coverage。
- 如果 GT-WA match@5 也低，主问题是 geometry/free_params/lattice。
- 如果 extraction_hard 在 GT-WA 下仍然低，优先查 free_param extraction/data quality。

输出：
reports/symcif_v4_mp20_k5_dev/gtwa_geometry_k5_eval.md
reports/symcif_v4_mp20_k5_dev/gtwa_geometry_k5_eval.json

========================================
任务 5：一个小规模模型/数据修正实验
========================================

基于任务 2-4 的结果，只选一个最可能有效的修正，不要同时开多个。

优先级：
A. 如果 selected top5 没选中但 raw candidates 有 GT WA：
   做 WA policy / candidate selection 改进：
   - K<=5 目标；
   - complex-weighted training；
   - hard negatives:
     same skeleton wrong assignment,
     same SG same n_sites wrong orbit,
     same multiplicity wrong site symmetry,
     n_sites>=6 false positives;
   - 评估 WA_hit@1/@5 和 full match@1/@5。

B. 如果 raw candidates 本身缺 GT WA：
   做 WA search/data augmentation：
   - 增大 raw WA candidates 到 50/100，但 final selected 仍然只取 5；
   - 不跑 match@20；
   - 加 complex subset sampling。

C. 如果 GT-WA geometry 也低：
   做 geometry/free_param 数据修正：
   - audit extraction_hard；
   - 减少 same_sg fallback；
   - 优先 same_skeleton / generated geometry；
   - 对 n_sites>=6 / extraction_hard 加权训练 geometry model。

只允许选择 A/B/C 中一个分支执行。

输出：
reports/symcif_v4_mp20_k5_dev/one_fix_experiment_summary.md
reports/symcif_v4_mp20_k5_dev/one_fix_experiment_summary.json

========================================
任务 6：最终报告
========================================

生成：
reports/symcif_v4_mp20_k5_dev/final_root_cause_summary.md

必须回答：
1. 当前 MP-20 K<=5 的主瓶颈是不是排序？
2. final top5 WA 覆盖够不够？
3. raw search 和 selected top5 哪个更有问题？
4. geometry source 是否异常，same_wa 为什么少？
5. GT-WA geometry K<=5 表现如何？
6. n_sites>=6 / extraction_hard 到底卡在 WA 还是 geometry？
7. 单个小修正是否提升 val match@1/match@5？
8. 是否值得再跑 MP-20 test K<=5？
9. 是否达到继续 MPTS-52 或 match@20 的条件？

停止规则：
- 不要跑 match@20。
- 不要跑 MPTS-52。
- 不要启动 CrystaLLM baseline。
- 不要同时开多个修正实验。
- 完成上述报告后停止。